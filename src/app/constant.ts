import { Theme } from './theme';

export const scope = '/portfolio';
export const cvFileName = 'CV Evaldas Laureckas.pdf';
export const routes = {
  dashboard: `${scope}/dashboard`
};
export const defaultTheme: Theme = Theme.mint;
