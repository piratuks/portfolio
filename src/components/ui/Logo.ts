import styled from 'styled-components';

export const LogoImgElement = styled.img`
  -webkit-filter: drop-shadow(1px 2px 25px rgba(var(--bs-dark-rgb), 0.9));
  filter: drop-shadow(1px 2px 25px rgba(var(--bs-dark-rgb), 0.9));
  cursor: pointer;
`;
